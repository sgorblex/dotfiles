; Sgorblex's Polybar config
; ~/.config/polybar/cute_modules/spotify
; bar: cute
; module: spotify
; requires polybar-spotify (AUR package polybar-spotify-git)


[module/spotify]
type = custom/script

interval = 1

format-prefix = " "
format = <label>
format-underline = #1db954

exec = python /usr/bin/polybar-spotify -t 35 -f '{artist}: {song}'


; vim:ft=dosini
